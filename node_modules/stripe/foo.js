var fs = require('fs');
var stripe = require('./lib/stripe')("");

fs.open('READ', 'r', function(err, _) {
  stripe.charges.list({}, function(err, _) {
    console.log((new Error()).stack);
    console.log(err.stack);
  });
});
